
export const OfflineModeButton = () => {
    return (
        <button
            className="text-[hsl(var(--foreground))] rounded-3xl border border-[hsl(var(--foreground))] py-3 px-6 text-center align-middle">
            Mode hors ligne
        </button>
    )
}